#### Remove entire folder through command - scripts is the folder to be removed in current directory
```
git rm -r scripts      
```

##### 'Removes multiple files'
```
git rm -r futures.js overunder.js pointspread.js  
```
